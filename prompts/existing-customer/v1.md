# Existing Customer Prompt — PLACEHOLDER
Replace this file with the deployed existing-customer flow prompt (behavior only — keep the
Knowledge Base in /kb). Keep the same versioned-filename convention: v1.md, v2.md, ...

Until the real prompt is added, evals run against the new-customer flow.
